import React from 'react';const Journal = () => {
    return (
      <>
        <form>
        {/* form needs onSubmit={this.handleSubmit} */}
          <label>
            <textarea value={} onChange={} />
          </label>
        </form>
      </>
    );
   }
   
   export default Journal;